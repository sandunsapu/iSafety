<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.isafetybots">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.isafetybots.navigation.PatientSettingsActivity"></activity>
        <activity android:name="com.example.isafetybots.navigation.PatientNotificationsActivity" />
        <activity android:name="com.example.isafetybots.navigation.PatientReportsActivity" />
        <activity android:name="com.example.isafetybots.navigation.PatientInstructionsActivity" />
        <activity android:name="com.example.isafetybots.PatientNavigationActivity" />
        <activity android:name="com.example.isafetybots.DoctorRegisterActivity" />
        <activity android:name="com.example.isafetybots.DoctorLoginActivity" />
        <activity android:name="com.example.isafetybots.PatientRegisterFinalActivity" />
        <activity android:name="com.example.isafetybots.PatientRegister4Activity" />
        <activity android:name="com.example.isafetybots.PatientRegister3Activity" />
        <activity android:name="com.example.isafetybots.PatientRegister2Activity" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity
            android:name="com.example.isafetybots.PatientRegisterActivity"
            android:label="@string/title_activity_patient_register" />
        <activity android:name="com.example.isafetybots.patientLoginActivity" />
        <activity
            android:name="com.example.isafetybots.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>