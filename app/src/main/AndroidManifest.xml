<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.isafetybots">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DoctorSendInstructionsActivity"></activity>
        <activity android:name=".DoctorPatientListActivity" />
        <activity android:name=".DoctorDashboardActivity" />
        <activity android:name=".DoctorRegisterFinalActivity" />
        <activity android:name=".PatientRequestCheckupActivity" />
        <activity android:name=".navigation.PatientSettingsActivity" />
        <activity android:name=".navigation.PatientNotificationsActivity" />
        <activity android:name=".navigation.PatientReportsActivity" />
        <activity android:name=".navigation.PatientInstructionsActivity" />
        <activity android:name=".NavigationPane.PatientNavigationActivity" />
        <activity android:name=".DoctorRegisterActivity" />
        <activity android:name=".DoctorLoginActivity" />
        <activity android:name=".PatientsRegister.PatientRegisterFinalActivity" />
        <activity android:name=".PatientsRegister.PatientRegister4Activity" />
        <activity android:name=".PatientsRegister.PatientRegister3Activity" />
        <activity android:name=".PatientsRegister.PatientRegister2Activity" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity
            android:name=".PatientsRegister.PatientRegisterActivity"
            android:label="@string/title_activity_patient_register" />
        <activity android:name=".PatientLoginActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>